# airflow-project


Проект по оркестрации выгрузки валют за различные даты с сервиса

```
https://exchangerate.host/documentation
```





# 05 
Изменил движок на Merge Tree. Потому что в log движок нельзя delete.
Перестал дропать таблицу каждый раз. Мне же дозапись нужна, а не дроп.
В dag указал, чтобы dag запускались по одному, чтобы не было конкуренции за запись в файл.

# 06
Использовал weight_rule = absolute, чтобы у task вес был только тот, который я задал, и не зависил от task до или после.

# 07
Использовал оператор SQLExecuteQueryOperator, который через коннектор postgres_default подключается к базе данных и выполняет sql запрос, который я укажу.

# 08
Для создания содключения к базе данных clickhouse параметры (host, port, login, password) будут получаться через base_hook
В этом примере все переменные скрыты в том числе пути к файлам.
Переменные создаются в UI airflow
Для получения переменых используется Variable.get()

# 09
Для передачи маленьких данных используется встроенный механизм Xcom. Обращаясь к объекту task, записываем в одну из баз метаданных airflow значение value, чтобы передать его из одной таски другой.
В этом примере получаетелем будет функция-фильтр, которая в зависимости от прочитанного из Xcom значения возращает определённое имя следующей таски. BranchPythonOperator, который получит эту функцию-фильтр, будет передавать управление таске, имя которой вернула функция-фильтр.
